name: Ligadaten aktualisieren

on:
  push:
  schedule:
    - cron: "0 4 * * *"
    - cron: "0 17 * * 5"
    - cron: "0 14 * * 2"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  holen:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: CSVs abrufen
        run: |
          mkdir -p daten
          LIGEN="E0 E1 E2 E3 EC SC0 SC1 SC2 SC3 D1 D2 I1 I2 SP1 SP2 F1 F2 N1 B1 P1 T1 G1"
          for SAISON in 2425 2526 2627; do
